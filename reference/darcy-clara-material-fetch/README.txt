Darcy-to-Clara material fetch inspection bundle. Contains the 12 source files identified during inspection, with original folder structure. The two .env.redacted files preserve variable names only; all values and comments are omitted. Source files are unchanged. This is an inspection bundle, not a standalone runnable application; dependencies and inventory datasets are not included.
